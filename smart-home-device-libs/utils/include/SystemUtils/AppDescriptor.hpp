#pragma once

#include <string>

struct AppDescriptor{
    std::string secureVersion;
    std::string appVersion;
    std::string projectName;
    std::string compileDate;
    std::string compoleTime;
    std::string idfVersion;
};